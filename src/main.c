#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "cache.h"

/*
 * There's still (per ChatGPT):
 * RAM memory (memory.c?)
 * MESI protocol
 * Definir processadores
 */

// Functions if needed

int main() {
    // declarations, init and whatnot

    return 0;
}
